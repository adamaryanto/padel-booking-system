<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Prevent directory listing
    Options -Indexes

    # Only rewrite if this is the initial request (prevents infinite loop on LiteSpeed/Apache)
    RewriteCond %{ENV:REDIRECT_STATUS} ^$
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
